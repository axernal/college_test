# college_test

# first_assignment
first we will find the frequency of all characters in string 
then return the first characters whose frequency matches to once 

if all cases fails and every character returns more than 1, then we will return -1 

# second_assignment
used XOR method where first we store xor of 1 to n+1 all natural numbers then 
store xor of numbers stores in array nums  
After that xor1 with xor2 all the similar elements will eliminate and only missing number will left  
